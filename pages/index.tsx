import type { NextPage } from "next";
import Layout from "../components/layout";

const Home: NextPage = () => {
  return (
    <Layout>
      <div>
        <h1>Home Screen</h1>
      </div>
    </Layout>
  );
};

export default Home;
